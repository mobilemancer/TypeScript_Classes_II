class DeathStar {
	static fireSuperLaser(): string {
		return "Wait, we're powering up...";
	}

	fireStuff(): string {
		return "Wait, we're powering up...";
	}
}

(function() {
	console.log(DeathStar.fireSuperLaser());
})();